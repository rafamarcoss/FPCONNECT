# Frontend (Vite)
# En desarrollo, la API también funciona mediante el proxy /api de Vite.
# Estas URLs son necesarias para el flujo OAuth y fijan explícitamente Socket.IO.
VITE_API_BASE_URL=http://localhost:3000/api
VITE_SOCKET_URL=http://localhost:3000
